# Structural Engineering Library - Consolidated Requirements
# ============================================================
# This file provides a unified installation for all dependencies.
# Source of truth for library deps: Python/pyproject.toml
# This file adds project-wide deps (FastAPI, scientific, docs).
#
# Quick Install (all features):
#   pip install -r requirements.txt
#
# Or install the package with optional dependencies:
#   pip install -e "Python[dev,dxf,render,report,pdf,validation,cad]"
#
# For development, also install pre-commit hooks:
#   ./scripts/python_runtime.sh -m pre_commit install
#
# Synced: 2026-04-04
# ============================================================

# ===========================================
# Core Library (required — matches pyproject.toml)
# ===========================================
pydantic>=2.0,<3.0

# ===========================================
# Development Tools (matches pyproject.toml [dev])
# ===========================================
pytest>=9.0
pytest-asyncio>=1.3,<2.0
pytest-cov>=7.0
pytest-benchmark>=5.0
black==26.5.1
mypy>=1.19,<2
pre-commit>=4.6.2
ruff==0.16.3
bandit>=1.9
hypothesis>=6.165.10

# ===========================================
# DXF/CAD Export (matches pyproject.toml [dxf])
# ===========================================
ezdxf>=1.0

# ===========================================
# Rendering & Visualization (matches pyproject.toml [render])
# ===========================================
matplotlib>=3.5

# ===========================================
# Reports (HTML/PDF — matches pyproject.toml [report] + [pdf])
# ===========================================
jinja2>=3.1
reportlab>=4.0
weasyprint>=62.0

# ===========================================
# Schema Validation (matches pyproject.toml [validation])
# ===========================================
jsonschema>=4.21

# ===========================================
# 3D CAD & Visualization (matches pyproject.toml [cad])
# ===========================================
pyvista>=0.43

# ===========================================
# Data & Scientific (project-wide, not in pyproject.toml)
# ===========================================
pandas>=2.0
numpy>=2.0,<2.5
scipy>=1.14

# ===========================================
# FastAPI Backend (project-wide, not in pyproject.toml)
# ===========================================
fastapi>=0.115,<1.0
uvicorn>=0.52.4
pydantic-settings>=2.15.0
# PyJWT for JWT authentication (migrated from python-jose, which is unmaintained)
PyJWT[crypto]>=2.13,<3.0
cryptography>=50.0,<51.0
python-multipart>=0.0.32
sse-starlette>=2.0
httpx>=0.27

# ===========================================
# Documentation (matches pyproject.toml [docs])
# ===========================================
mkdocs-material>=9.5
mkdocstrings[python]>=0.25
websockets>=17.0.1

# ===========================================
# Typing (backports for Python 3.11 compatibility)
# ===========================================
typing-extensions>=4.12
