name: Link Check

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - '*.md'
      - '.github/workflows/link-check.yml'
  pull_request:
    paths:
      - 'docs/**'
      - '*.md'
  schedule:
    - cron: '0 6 * * 1'  # Weekly on Monday 6am UTC

permissions:
  contents: read

jobs:
  link-check:
    name: Check Links
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Link Checker
        uses: lycheeverse/lychee-action@v2
        with:
          args: >-
            --no-progress
            --exclude 'mailto:'
            --exclude 'localhost'
            --exclude '127.0.0.1'
            --exclude 'example.com'
            --exclude 'unpkg.com/katex'
            --exclude 'doi.org'
            --exclude 'researchgate.net'
            --exclude 'rubberduckvba.com'
            --exclude 'draftsmagic.com'
            --exclude 'docs.structural-engineering-lib.io'
            --exclude 'docs.structural-lib.com'
            --exclude 'github.com/owner/repo'
            --exclude 'github.com/\.\.\.'
            --exclude 'github.com/pravin-surawase/structural-lib/discussions'
            --exclude 'medium.com'
            --timeout 30
            --retry-wait-time 5
            --max-retries 3
            --exclude-path 'docs/_archive'
            --exclude-path 'docs/research'
            --exclude-path 'docs/blog-drafts'
            --exclude-path 'docs/publications'
            --suggest
            'docs/**/*.md'
            '*.md'
          fail: true

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: link-check-report
          path: lychee/out.md
          retention-days: 30
