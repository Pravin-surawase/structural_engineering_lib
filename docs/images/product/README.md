# Product Screenshots

Authentic StructLib v0.23.0 captures used by the root README and product tour.

| File | Screen | Verified state |
|---|---|---|
| `import-preview.jpg` | Import preview | 153 bundled sample beams, six stories, all with 3D positions |
| `building-editor.jpg` | Building Editor | Imported frame, batch results table, no member selected |
| `beam-inspector.jpg` | Beam inspector | Selected sample beam with reinforcement and clause checks |
| `design-dashboard.jpg` | Dashboard | Completed bundled-sample summary and BOQ view |

## Capture notes

- Captured at 1280×720 from the locally running React and FastAPI stack.
- Verified with the bundled ETABS sample on 2026-08-10.
- The browser reported no warning/error entries or framework error overlay in
  the captured states.
- Files are JPEG because that is the browser capture format.
- Captures are product evidence only; they do not certify design accuracy or
  authorize engineering use.

When the interface changes materially, recapture the same four states and
update this note so public visuals do not drift from the shipped product.
