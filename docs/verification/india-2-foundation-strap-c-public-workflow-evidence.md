---
owner: Main Agent
status: active
last_updated: 2026-08-16
doc_type: verification
task: INDIA-2-FOUNDATION-STRAP-C
---

# INDIA-2 Foundation Strap C Public Python Evidence

## Published Python contract

`design_property_line_strap_footing_is456` is the sole canonical public Python
composition over STRAP-A/B. It accepts one immutable case wrapper around the
typed design input and returns immutable `PASS`/`FAIL`, strength, supported/
held case, provenance, qualified-review, and false complete-engineering-
approval fields. The same object/function identities are exported from the
package root, services package, services API hub, and compatibility stub.

The mapping builder reconstructs the complete nested contract for STRAP-D
without transport-layer structural math. The frozen benchmark remains `PASS`;
a valid bearing inadequacy returns public `FAIL`; wrong types, blank case ID,
missing review acknowledgement, and malformed mappings fail closed.

## Provenance and truth boundary

Provenance carries the schema/code/workflow/case/benchmark IDs, action and soil
status, all geometry/rigidity/isolation/load/bearing/carrier/line-load/pattern
bases, both footing verification references, transfer/construction references,
material/detailing/durability references, clause identities, controlled source
hashes, public IISc/NPTEL identity, and distribution permission identity.

The deterministic manifest truthfully records this family as
`HELD / IMPLEMENTED_BOUNDED` with the Python workflow and G0/A/B/C evidence.
FastAPI transport, supported capability registration, semantic publication,
focused family acceptance, React, release, and professional approval remain
held. Counts remain `12 supported / 9 held` and 80/80 endpoints.

## Verification

- Direct public Python composition, canonical-export, serialization,
  provenance, valid-failure, fail-closed, builder, deterministic/frozen, and
  held-truth tests pass.
- Cumulative A/B/C, public contract, API docs/signatures, Ruff, Black, mypy,
  architecture/import, manifest/parity, links/indexes, source binding, and
  quick gate pass.
- Broad Python and the full 30-check gate remain deferred to the final INDIA-2
  integration boundary unless a confirmed repository-wide failure forces
  either earlier.
- Exact candidate commit/tree, independent audit, hosted checks, and merge
  identity are bound by the packet Git handoff receipt.
