---
owner: Main Agent
status: active
last_updated: 2026-08-17
doc_type: reference
complexity: advanced
tags: [a1, api, transport, result, evidence]
---

# A1 Canonical Truth and Transport Evidence

**Evidence boundary:** Packet A1 implementation and frozen local verification
selection only. This record is not A2 or G1 acceptance, gravity authorization,
release authorization, hosted-check evidence, professional approval, or a claim
that a pending check has passed.

## Authorization and source

- The owner approved G0 and directed the writer to start A1 on 2026-08-17.
- Branch: `codex/a1-canonical-transport-contract`.
- Base/head before the candidate commit:
  `09861d3d5ef758abbe0f7c40b8b49b2f90510765`.
- Runtime diagnosis: `source_bound=true` in the isolated linked worktree.

## A1 contract closure

The reference `design_beam_is456` journey has one shared:

- `effective-depth-basis/v1` resolver with an explicit or complete derived basis;
- `structural-result-envelope/v2` with independent intake, calculation,
  engineering, freshness, review, issues, and replay identity;
- `structural-problem/v1` for rejected JSON operations;
- strict JSON normalizer that rejects non-finite values;
- runtime version authority that distinguishes source checkout from installed
  distribution metadata; and
- `claim-surface-matrix/v1` classification and artifact boundary.

The migrated maintained surfaces are the root, service, and compatibility Python
facades; retained CLI; FastAPI beam and workflow routes; OpenAPI; React manual,
catalogue, workflow, and trust presentation; and checked-in Python/TypeScript
development clients. Compatibility delegates remain delegates.

## Explicit holds and non-goals

- A WebSocket payload without a valid v2 result envelope is presented as `HOLD`;
  its legacy calculation payload is not silently promoted to canonical PASS.
- ETABS CSV preview exports are classified `hold`; no live ETABS connection or
  write-back was started.
- A2 lossless-intake/load-math repairs, gravity orchestration, Excel integration,
  optimization, global solver work, tag, upload, and release are outside A1.

## Frozen verification selection

Run after content and maintained projections freeze:

1. One focused Python/FastAPI pytest invocation covering result, error, unit,
   identity, serialization, depth, workflow, client, claim-classification,
   readiness-truth, CLI, slab/column compatibility, and the reference HTTP vector.
2. One focused React invocation for the changed catalogue, workflow, API client,
   WebSocket, store, live-design, and trust-presentation tests; then one TypeScript
   compile and lint of the exact-head app.
3. API parity, API/classification/tool-manifest deterministic checks, and the
   OpenAPI snapshot check.
4. Build one wheel outside the repository and run
   `scripts/verify_canonical_transport_artifact.py` for source-free Python/CLI and
   exact-head FastAPI-to-wheel import binding.
5. One quick gate, one normal commit-hook run, one immutable exact-head audit,
   one push, and one hosted validation closeout.

Failed checks are diagnosed as one related repair batch. Only failed or
impact-mapped evidence is repeated after an outcome-changing repair. A second
material rejection returns A1 to planning.

## Efficiency receipt targets

| Receipt | A1 target |
|---|---:|
| Unchanged-suite reruns | 0 |
| Focused verification batches | 1 |
| Quick gates | 1 |
| Full Python suite | 0 |
| Full repository gate | 0 |
| Maintained index refreshes | 1 |
| Hosted validation runs | 1 |

The broad Python suite and full repository gate belong to cumulative M2/G1
closeout after A2, not this packet.

## Local evidence through the revised content freeze

- The frozen focused Python/FastAPI selection passed. The focused React
  selection passed with 10 files and 87 tests.
- The first production TypeScript build rejected one imprecise draft cast and
  two pre-contract test fixtures. The impact-mapped repair passed 7 tests,
  lint, and the production build.
- A second quality rejection returned A1 to planning as required. The bounded
  repair restored optional-dependency version lookup, removed an unused depth
  validation assignment, and normalized changed-file formatting. Its
  impact-mapped proof passed 108 tests, all 3 transport-parity vectors, Ruff,
  Black, frontmatter with zero invalid records, and all 1,363 links.
- The first candidate-hook attempt created no commit. It exposed two mypy
  narrowing gaps, one evidence tuple annotation gap, two missing public API
  documentation tokens, and the unregistered artifact verifier. After that
  bounded replan, mypy passed 236 source files, public API docs passed, and the
  automation registry covered 111 of 111 scripts.
- The maintained contract suite then exposed an unintended signature break:
  `d_mm` had become omittable. The final contract keeps the keyword required
  while accepting `d_mm=None` only with a complete depth basis. Its proof passed
  42 affected tests, 18 frozen contract tests, and all 3 parity vectors.
- API manifest, API classification, beam tool manifest, and OpenAPI projections
  are deterministic and current. OpenAPI contains 82 endpoints and 362 schemas.
- The external wheel artifact proof passed for source-free Python, source-free
  CLI, and exact-head FastAPI bound to the installed wheel. The boundary vector
  used 443 mm effective depth and returned engineering `FAIL` consistently.
  The final rebuilt wheel reported installed-distribution version `0.23.1a2`
  and SHA-256
  `2c943b2bbb61c8a2572ee7118b08b5650628ea25b83c2f87a71ed31afc359793`.

The quick gate, normal commit hooks, immutable exact-head audit, push, and
hosted validation occur after this content freeze and are reported externally.

## Efficiency receipt through content freeze

| Receipt | Actual |
|---|---:|
| Unchanged-suite reruns | 0 |
| Frozen focused verification batches | 1 |
| Impact-mapped repair/replan batches | 4 |
| Source-free artifact builds | 2 |
| Quick gates | 1 |
| Candidate-hook attempts | 1 rejected; 0 committed |
| Full Python suite | 0 |
| Full repository gate | 0 |
| Maintained index write batches after repaired content | 3 |
| Hosted validation runs | 0 |
