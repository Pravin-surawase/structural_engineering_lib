# Secrets configuration for StructEng Streamlit App
# Copy this file to secrets.toml and fill in your API keys.
#
# IMPORTANT: Never commit secrets.toml to version control!
# The actual secrets.toml is already in .gitignore.
#
# For deployment (Streamlit Cloud), add these secrets in the app settings.

# OpenAI API Key for AI Chat Assistant
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY = "sk-your-api-key-here"

# Optional: Anthropic API Key (for future Claude integration)
# ANTHROPIC_API_KEY = "sk-ant-your-api-key-here"

# Model settings (uncomment to customize)
# Available models (January 2026):
#   - gpt-5-mini: Fast, cost-efficient ($0.25/1M tokens) - RECOMMENDED
#   - gpt-5: Full reasoning model ($1.75/1M tokens)
#   - gpt-5.2: Best for coding/agentic tasks
#   - gpt-4.1: Non-reasoning, fast
#   - gpt-4o-mini: Older, still good for simple tasks
[openai]
model = "gpt-5-mini"
temperature = 0.7
max_tokens = 2000
