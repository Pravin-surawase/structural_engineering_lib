# Secrets configuration for StructEng Streamlit App
# Copy this file to secrets.toml and fill in your API keys.
#
# IMPORTANT: Never commit secrets.toml to version control!
# The actual secrets.toml is already in .gitignore.
#
# For deployment (Streamlit Cloud), add these secrets in the app settings.

# API Key for AI Chat Assistant
# Supports both OpenAI and OpenRouter:
#
# Option 1: OpenAI (direct)
#   Get your key at: https://platform.openai.com/api-keys
#   Format: sk-...
#   OPENAI_API_KEY = "sk-your-api-key-here"
#
# Option 2: OpenRouter (many models, pay-as-you-go)
#   Get your key at: https://openrouter.ai/keys
#   Format: sk-or-v1-...
#   OPENAI_API_KEY = "sk-or-v1-your-key-here"
#
OPENAI_API_KEY = "sk-your-api-key-here"

# Model settings
# For OpenAI: gpt-4o-mini, gpt-4o, gpt-4-turbo
# For OpenRouter: use full path like openai/gpt-4o-mini, anthropic/claude-3-haiku
[openai]
model = "openai/gpt-4o-mini"
temperature = 0.7
max_tokens = 2000
