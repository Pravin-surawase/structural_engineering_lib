<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beam Design Report - {{ beam_id }}</title>
    <style>
        /* Reset and base styles */
        * { box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            margin: 0;
            padding: 20px;
            font-size: 14px;
            line-height: 1.5;
            color: #333;
            background: #fff;
        }

        /* Print styles */
        @media print {
            body { padding: 0; font-size: 12px; }
            .no-print { display: none !important; }
            .section { break-inside: avoid; }
        }

        /* Header */
        .header {
            border-bottom: 3px solid #2c3e50;
            padding-bottom: 15px;
            margin-bottom: 25px;
        }
        .header-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 20px;
        }
        .project-info h1 {
            margin: 0 0 10px;
            font-size: 24px;
            color: #2c3e50;
        }
        .project-info p {
            margin: 4px 0;
            color: #555;
        }
        .meta-box {
            border: 1px solid #ddd;
            padding: 12px;
            font-size: 12px;
            background: #f8f9fa;
        }
        .meta-box table { width: 100%; border-collapse: collapse; }
        .meta-box td { padding: 4px 6px; }
        .meta-box td:first-child { font-weight: 600; width: 45%; color: #555; }

        /* Status badge */
        .status-badge {
            display: inline-block;
            padding: 6px 16px;
            border-radius: 4px;
            font-weight: 700;
            font-size: 14px;
            text-transform: uppercase;
        }
        .status-pass { background: #d4edda; color: #155724; }
        .status-fail { background: #f8d7da; color: #721c24; }

        /* Sections */
        .section {
            margin: 20px 0;
            padding: 18px;
            background: #fafafa;
            border: 1px solid #e9ecef;
            border-radius: 6px;
        }
        .section h2 {
            margin: 0 0 15px;
            font-size: 18px;
            color: #2c3e50;
            border-bottom: 1px solid #dee2e6;
            padding-bottom: 8px;
        }
        .section h3 {
            margin: 15px 0 10px;
            font-size: 15px;
            color: #495057;
        }

        /* Tables */
        table.data-table {
            width: 100%;
            border-collapse: collapse;
            margin: 10px 0;
        }
        table.data-table th,
        table.data-table td {
            padding: 8px 12px;
            text-align: left;
            border-bottom: 1px solid #dee2e6;
        }
        table.data-table th {
            background: #e9ecef;
            font-weight: 600;
            color: #495057;
        }
        table.data-table tr:hover { background: #f5f6f7; }
        table.data-table td.value { text-align: right; font-family: monospace; }
        table.data-table td.unit { color: #6c757d; width: 60px; }

        /* Check items */
        .check-item {
            display: flex;
            align-items: center;
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }
        .check-item:last-child { border-bottom: none; }
        .check-icon {
            width: 24px;
            height: 24px;
            border-radius: 50%;
            margin-right: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 14px;
        }
        .check-pass { background: #d4edda; color: #155724; }
        .check-fail { background: #f8d7da; color: #721c24; }
        .check-label { flex: 1; }
        .check-value { font-family: monospace; color: #495057; }

        /* Footer */
        .footer {
            margin-top: 30px;
            padding-top: 15px;
            border-top: 1px solid #dee2e6;
            font-size: 11px;
            color: #6c757d;
            text-align: center;
        }

        /* Two column layout */
        .two-col {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        @media (max-width: 768px) {
            .two-col { grid-template-columns: 1fr; }
            .header-grid { grid-template-columns: 1fr; }
        }

        /* Code reference */
        .code-ref {
            background: #f1f3f5;
            padding: 2px 6px;
            border-radius: 3px;
            font-family: monospace;
            font-size: 12px;
        }
    </style>
</head>
<body>
    <!-- Header Section -->
    <header class="header">
        <div class="header-grid">
            <div class="project-info">
                <h1>Beam Design Report: {{ beam_id }}</h1>
                <p><strong>Project:</strong> {{ project_name }}</p>
                {% if project_number %}<p><strong>Project No:</strong> {{ project_number }}</p>{% endif %}
                {% if client_name %}<p><strong>Client:</strong> {{ client_name }}</p>{% endif %}
            </div>
            <div class="meta-box">
                <table>
                    <tr><td>Code:</td><td>{{ code_reference }}</td></tr>
                    <tr><td>Date:</td><td>{{ date }}</td></tr>
                    <tr><td>Revision:</td><td>{{ revision }}</td></tr>
                    {% if engineer_name %}<tr><td>Engineer:</td><td>{{ engineer_name }}</td></tr>{% endif %}
                    {% if checker_name %}<tr><td>Checker:</td><td>{{ checker_name }}</td></tr>{% endif %}
                </table>
            </div>
        </div>
        <div style="margin-top: 15px;">
            <span class="status-badge {{ status_class }}">{{ status_text }}</span>
        </div>
    </header>

    <!-- Input Data Section -->
    <section class="section">
        <h2>1. Input Data</h2>
        <div class="two-col">
            <div>
                <h3>Geometry</h3>
                <table class="data-table">
                    <tr>
                        <th>Parameter</th>
                        <th>Value</th>
                        <th>Unit</th>
                    </tr>
                    {% if inputs.geometry %}
                    {% for key, value in inputs.geometry.items() %}
                    <tr>
                        <td>{{ key | replace('_', ' ') | title }}</td>
                        <td class="value">{{ value | format_number }}</td>
                        <td class="unit">mm</td>
                    </tr>
                    {% endfor %}
                    {% else %}
                    <tr>
                        <td>Width (b)</td>
                        <td class="value">{{ inputs.get('b_mm', inputs.get('b', '-')) | format_number }}</td>
                        <td class="unit">mm</td>
                    </tr>
                    <tr>
                        <td>Depth (D)</td>
                        <td class="value">{{ inputs.get('D_mm', inputs.get('D', '-')) | format_number }}</td>
                        <td class="unit">mm</td>
                    </tr>
                    <tr>
                        <td>Effective Depth (d)</td>
                        <td class="value">{{ inputs.get('d_mm', inputs.get('d', '-')) | format_number }}</td>
                        <td class="unit">mm</td>
                    </tr>
                    <tr>
                        <td>Clear Span</td>
                        <td class="value">{{ inputs.get('span_mm', inputs.get('span', '-')) | format_number }}</td>
                        <td class="unit">mm</td>
                    </tr>
                    {% endif %}
                </table>
            </div>
            <div>
                <h3>Materials</h3>
                <table class="data-table">
                    <tr>
                        <th>Parameter</th>
                        <th>Value</th>
                        <th>Unit</th>
                    </tr>
                    {% if inputs.materials %}
                    <tr>
                        <td>Concrete Grade (f<sub>ck</sub>)</td>
                        <td class="value">{{ inputs.materials.get('fck', '-') | format_number }}</td>
                        <td class="unit">N/mm²</td>
                    </tr>
                    <tr>
                        <td>Steel Grade (f<sub>y</sub>)</td>
                        <td class="value">{{ inputs.materials.get('fy', '-') | format_number }}</td>
                        <td class="unit">N/mm²</td>
                    </tr>
                    {% else %}
                    <tr>
                        <td>Concrete Grade (f<sub>ck</sub>)</td>
                        <td class="value">{{ inputs.get('fck', '-') | format_number }}</td>
                        <td class="unit">N/mm²</td>
                    </tr>
                    <tr>
                        <td>Steel Grade (f<sub>y</sub>)</td>
                        <td class="value">{{ inputs.get('fy', '-') | format_number }}</td>
                        <td class="unit">N/mm²</td>
                    </tr>
                    {% endif %}
                </table>
            </div>
        </div>
    </section>

    <!-- Flexure Design Section -->
    <section class="section">
        <h2>2. Flexural Design <span class="code-ref">IS 456 Cl. 38.1</span></h2>

        {% if results.flexure %}
        <table class="data-table">
            <tr>
                <th>Parameter</th>
                <th>Value</th>
                <th>Unit</th>
                <th>Remarks</th>
            </tr>
            <tr>
                <td>Design Moment (M<sub>u</sub>)</td>
                <td class="value">{{ results.flexure.get('mu_knm', '-') | format_number }}</td>
                <td class="unit">kN·m</td>
                <td></td>
            </tr>
            <tr>
                <td>Limiting Moment (M<sub>u,lim</sub>)</td>
                <td class="value">{{ results.flexure.get('mu_lim_knm', '-') | format_number }}</td>
                <td class="unit">kN·m</td>
                <td></td>
            </tr>
            <tr>
                <td>Steel Required (A<sub>st</sub>)</td>
                <td class="value">{{ results.flexure.get('ast_required_mm2', results.flexure.get('ast_mm2', '-')) | format_number }}</td>
                <td class="unit">mm²</td>
                <td></td>
            </tr>
            <tr>
                <td>Steel Provided</td>
                <td class="value">{{ results.flexure.get('ast_provided_mm2', '-') | format_number }}</td>
                <td class="unit">mm²</td>
                <td>{{ results.flexure.get('bar_arrangement', '') }}</td>
            </tr>
            <tr>
                <td>Reinforcement Ratio (ρ)</td>
                <td class="value">{{ results.flexure.get('pt_percent', '-') | format_number }}</td>
                <td class="unit">%</td>
                <td></td>
            </tr>
        </table>

        <div class="check-item">
            {% if results.flexure.get('is_ok', True) %}
            <div class="check-icon check-pass">✓</div>
            {% else %}
            <div class="check-icon check-fail">✗</div>
            {% endif %}
            <span class="check-label">Flexure Design</span>
            <span class="check-value">{{ 'PASS' if results.flexure.get('is_ok', True) else 'FAIL' }}</span>
        </div>
        {% else %}
        <p><em>Flexure results not available</em></p>
        {% endif %}
    </section>

    <!-- Shear Design Section -->
    <section class="section">
        <h2>3. Shear Design <span class="code-ref">IS 456 Cl. 40</span></h2>

        {% if results.shear %}
        <table class="data-table">
            <tr>
                <th>Parameter</th>
                <th>Value</th>
                <th>Unit</th>
            </tr>
            <tr>
                <td>Design Shear (V<sub>u</sub>)</td>
                <td class="value">{{ results.shear.get('vu_kn', '-') | format_number }}</td>
                <td class="unit">kN</td>
            </tr>
            <tr>
                <td>Shear Stress (τ<sub>v</sub>)</td>
                <td class="value">{{ results.shear.get('tv_nmm2', '-') | format_number }}</td>
                <td class="unit">N/mm²</td>
            </tr>
            <tr>
                <td>Design Shear Strength (τ<sub>c</sub>)</td>
                <td class="value">{{ results.shear.get('tc_nmm2', '-') | format_number }}</td>
                <td class="unit">N/mm²</td>
            </tr>
            <tr>
                <td>Maximum Shear Stress (τ<sub>c,max</sub>)</td>
                <td class="value">{{ results.shear.get('tc_max_nmm2', '-') | format_number }}</td>
                <td class="unit">N/mm²</td>
            </tr>
            <tr>
                <td>Stirrup Spacing Required</td>
                <td class="value">{{ results.shear.get('sv_required_mm', '-') | format_number(0) }}</td>
                <td class="unit">mm</td>
            </tr>
            <tr>
                <td>Stirrup Spacing Provided</td>
                <td class="value">{{ results.shear.get('sv_provided_mm', '-') | format_number(0) }}</td>
                <td class="unit">mm</td>
            </tr>
        </table>

        <div class="check-item">
            {% if results.shear.get('is_ok', True) %}
            <div class="check-icon check-pass">✓</div>
            {% else %}
            <div class="check-icon check-fail">✗</div>
            {% endif %}
            <span class="check-label">Shear Design</span>
            <span class="check-value">{{ 'PASS' if results.shear.get('is_ok', True) else 'FAIL' }}</span>
        </div>
        {% else %}
        <p><em>Shear results not available</em></p>
        {% endif %}
    </section>

    <!-- Serviceability Section -->
    {% if results.deflection or results.crack_width %}
    <section class="section">
        <h2>4. Serviceability Checks <span class="code-ref">IS 456 Cl. 42, 43</span></h2>

        {% if results.deflection %}
        <h3>Deflection Check</h3>
        <div class="check-item">
            {% if results.deflection.get('is_ok', True) %}
            <div class="check-icon check-pass">✓</div>
            {% else %}
            <div class="check-icon check-fail">✗</div>
            {% endif %}
            <span class="check-label">Span/Depth Ratio</span>
            <span class="check-value">
                {{ results.deflection.get('ld_actual', '-') | format_number }}
                ≤ {{ results.deflection.get('ld_allowable', '-') | format_number }}
            </span>
        </div>
        {% endif %}

        {% if results.crack_width %}
        <h3>Crack Width Check</h3>
        <div class="check-item">
            {% if results.crack_width.get('is_ok', True) %}
            <div class="check-icon check-pass">✓</div>
            {% else %}
            <div class="check-icon check-fail">✗</div>
            {% endif %}
            <span class="check-label">Crack Width</span>
            <span class="check-value">
                {{ results.crack_width.get('wk_mm', '-') | format_number(3) }} mm
                ≤ {{ results.crack_width.get('wk_limit_mm', '-') | format_number(2) }} mm
            </span>
        </div>
        {% endif %}
    </section>
    {% endif %}

    <!-- Detailing Section -->
    {% if results.detailing %}
    <section class="section">
        <h2>5. Detailing <span class="code-ref">IS 456 Cl. 26</span></h2>

        <div class="two-col">
            <div>
                <h3>Reinforcement</h3>
                <table class="data-table">
                    <tr>
                        <th>Location</th>
                        <th>Bars</th>
                    </tr>
                    {% if results.detailing.get('bottom_bars') %}
                    <tr>
                        <td>Bottom (Tension)</td>
                        <td>{{ results.detailing.get('bottom_bars', '-') }}</td>
                    </tr>
                    {% endif %}
                    {% if results.detailing.get('top_bars') %}
                    <tr>
                        <td>Top (Compression)</td>
                        <td>{{ results.detailing.get('top_bars', '-') }}</td>
                    </tr>
                    {% endif %}
                    {% if results.detailing.get('stirrups') %}
                    <tr>
                        <td>Stirrups</td>
                        <td>{{ results.detailing.get('stirrups', '-') }}</td>
                    </tr>
                    {% endif %}
                </table>
            </div>
            <div>
                <h3>Development Length</h3>
                <table class="data-table">
                    <tr>
                        <td>L<sub>d</sub> (Tension)</td>
                        <td class="value">{{ results.detailing.get('ld_tension_mm', '-') | format_number(0) }}</td>
                        <td class="unit">mm</td>
                    </tr>
                    {% if results.detailing.get('lap_length_mm') %}
                    <tr>
                        <td>Lap Length</td>
                        <td class="value">{{ results.detailing.get('lap_length_mm', '-') | format_number(0) }}</td>
                        <td class="unit">mm</td>
                    </tr>
                    {% endif %}
                </table>
            </div>
        </div>
    </section>
    {% endif %}

    <!-- Summary Section -->
    <section class="section">
        <h2>6. Design Summary</h2>

        <div class="check-item">
            {% if is_ok %}
            <div class="check-icon check-pass">✓</div>
            {% else %}
            <div class="check-icon check-fail">✗</div>
            {% endif %}
            <span class="check-label"><strong>Overall Design Status</strong></span>
            <span class="check-value"><strong>{{ status_text }}</strong></span>
        </div>

        {% if results.get('errors') %}
        <div style="margin-top: 15px; padding: 10px; background: #f8d7da; border-radius: 4px;">
            <strong>Errors:</strong>
            <ul style="margin: 5px 0;">
            {% for error in results.errors %}
                <li>{{ error }}</li>
            {% endfor %}
            </ul>
        </div>
        {% endif %}

        {% if results.get('warnings') %}
        <div style="margin-top: 15px; padding: 10px; background: #fff3cd; border-radius: 4px;">
            <strong>Warnings:</strong>
            <ul style="margin: 5px 0;">
            {% for warning in results.warnings %}
                <li>{{ warning }}</li>
            {% endfor %}
            </ul>
        </div>
        {% endif %}
    </section>

    <!-- Footer -->
    <footer class="footer">
        <p>
            Generated by structural-lib-is456
            {% if software_version %} v{{ software_version }}{% endif %}
            | {{ code_reference }}
            | {{ date }}
        </p>
        <p style="margin-top: 5px;">
            <em>This report is computer-generated. The designing engineer is responsible for verifying all calculations.</em>
        </p>
    </footer>
</body>
</html>
